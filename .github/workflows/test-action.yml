name: test-action

on: push
  workflow_dispatch:

jobs:
  test-action-job:

    runs-on: ubuntu-latest

    steps: 
      - name: checkout code
        uses: action/checkout@v4
      - run: echo "$(cat another-test-script.txt)"
